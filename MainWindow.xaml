<Window x:Class="Hackathon.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackathon"
        xmlns:GuideEntry="clr-namespace:Hackathon"
        xmlns:mapsui="clr-namespace:Mapsui.UI.Wpf;assembly=Mapsui.UI.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Loaded="Window_Loaded"
        SizeChanged="Window_SizeChanged">

    <Grid x:Name="GridMain">
        <TabControl>
            <TabItem x:Name="TabGuide" Header="Guide">
                <Grid Background="#FFE5E5E5">
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal" Background="Gray" Height="40" VerticalAlignment="Center">
                            <Label Margin="0,6,0,0" Content="Search" FontWeight="Bold"/>
                            <TextBox x:Name="TextBoxSearch" Width="700" Height="20" TextChanged="TextBoxSearch_TextChanged"/>
                        </StackPanel>
                        <StackPanel x:Name="StackPannelGuides" HorizontalAlignment="Stretch" Margin="20" Height="340">
                            <ScrollViewer x:Name="ScrawllBarGuides">
                                <StackPanel Name="StackPanellGuideEntry">
                                </StackPanel>
                            </ScrollViewer>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem x:Name="TabMap" Header="Map" HorizontalAlignment="Center">
                <Grid Background="#FFE5E5E5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="40"/>
                    </Grid.RowDefinitions>
                    <mapsui:MapControl x:Name="mapControl" Grid.Row="0"/>
                    <Button Width="100" Height="35" Content="Next Point" x:Name="NextButton"
                            Grid.Row="1" HorizontalAlignment="Left"
                            Click="NextButton_Click"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>